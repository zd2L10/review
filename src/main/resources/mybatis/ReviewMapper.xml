<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.app.mapper.ReviewMapper">
	
	<resultMap id="joinedReview" type="Review" autoMapping="true">
		<association property="user" javaType="User" autoMapping="true" />
	</resultMap>
	
	<select id="selectAll" resultMap="joinedReview">
		SELECT * FROM review 
		JOIN user 
		ON review.name = user.name
	</select>
	
	<select id="selectByName" parameterType="String" resultMap="joinedReview">
		SELECT * FROM review
		JOIN user
		ON review.name = user.name
		WHERE review.name = #{name}
	</select>
	
	<insert id="insert" parameterType="Review">
		INSERT INTO review 
		(title, img, name, review)
		VALUES 
		(#{title}, #{img}, #{name}, #{review})
	</insert>
	
	<update id="update" parameterType="Review">
		UPDATE result 
		SET title = #{title}, img = #{img}, 
		name = #{name}, review = #{review}
		WHERE review_id = #{reviewId}
	</update>
	
	<delete id="delete" >
		DELETE FROM review 
		WHERE review_id = #{reviewId}
	</delete>
	
	<select id="findById" resultMap="joinedReview">
		SELECT 
		result.result_id, result.user_id, user.login_id, 
		result.derection, result.rank, result.time, 
		result.east_score, result.south_score, 
		result.west_score, result.north_score, 
		result.east_player, result.south_player, 
		result.west_player, result.north_player 
		FROM result 
		JOIN user 
		ON result.user_id = user.id 
		WHERE result_id = #{resultId}
	</select>
	
	<!-- ページ分割 -->
	<select id="countReview" resultType="long">
	SELECT COUNT(*)
	FROM review
	</select>
	
	<select id="selectLimited" resultType="Review">
	SELECT * FROM review
	LIMIT #{offset}, #{limit}
	</select>
</mapper>